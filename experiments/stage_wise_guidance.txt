Stage0:
Verify with "python verify_mcp_connection.py"

Stage1 + Stage2:
Verify with:
1. "python -m otel-griptape/tests/test_instrumentor.py" #(run this from otel-griptape directory)
2. "python app.py --question "What does the Kestrel Basin report say about sea level rise and drought?" --pdf fixtures/long_climate_report.pdf" # (run this from demo-agent-app directory and make sure to run "python demo-agent-app/fixtures/generate_fixture_pdf.py" once to get the required long_climate_report.pdf)
3. In the SigNoz UI, go to Traces, filter by your service name, and open the trace from that run.
    Check the three literal gate criteria:
        (i) Token counts present — expand a chat <model> span, confirm gen_ai.usage.input_tokens / gen_ai.usage.output_tokens show real (non-zero) numbers.
        (ii) Model name present — same span, confirm gen_ai.request.model matches what you actually called (e.g. gpt-4o-mini).
        (iii) One connected trace tree — the whole run (Planner span → Fetch&Read → Fact-Check's parallel tool-call spans → Report Writer) should appear as a single trace with a single root, not split into multiple disconnected traces. Pay particular attention to the Fact-Check spans specifically — those are the ones that would go orphaned if the ThreadPoolExecutor.submit patch weren't working.
    Repeat for 2 more runs (different questions/PDFs) — the spec asks for 3 sample traces, since one clean run could be a fluke and won't catch e.g. an intermittent context-propagation miss under real concurrency/timing (which is exactly the kind of bug that behaves differently than in my synthetic in-memory test).

Stage3:
First run the "CHAOS_MODE=1 CHAOS_R1_RATE=1.0 CHAOS_R2_RATE=1.0 CHAOS_SEED=42 python app.py --question "What does the Kestrel Basin report say about sea level rise and drought?" --pdf fixtures/long_climate_report.pdf > chaos_out.json"
OR
For no chaos (baseline) run this: "python app.py --question "What does the Kestrel Basin report say about sea level rise and drought?" --pdf fixtures/long_climate_report.pdf > baseline_out.json"

Then verify with "python test_stage3.py" from project root. Copy this file to project root and then run.

Stage4:
Unit tests first (no MCP/network needed): "pytest guardian/tests/test_r3_orphaned_spans.py guardian/tests/test_r6_silent_truncation.py" from project root.

Then, against a live SigNoz run -- first a clean baseline, then a chaos run isolating R3+R6 specifically (R1/R2 rates zeroed so their findings don't clutter the read):
"python app.py --question \"What does the Kestrel Basin report say about sea level rise and drought?\" --pdf fixtures/long_climate_report.pdf > baseline_out.json"
"CHAOS_MODE=1 CHAOS_R1_RATE=0 CHAOS_R2_RATE=0 CHAOS_R3_RATE=1.0 CHAOS_R6_RATE=1.0 CHAOS_SEED=42 python app.py --question "What does the Kestrel Basin report say about sea level rise and drought?" --pdf fixtures/long_climate_report.pdf > chaos_out.json"

Then verify with "python test_stage4.py" from project root (copy experiments/test_stage4.py to project root first, same as test_stage3.py).

Stage5:

Unit tests first (no MCP, no real LLM call -- litellm.completion is mocked):
    pip install -e guardian[test] litellm   # if not already installed
    python -m pytest guardian/tests/test_llm_client.py guardian/tests/test_narrative.py

"CHAOS_MODE=1 CHAOS_R1_RATE=1 CHAOS_R2_RATE=1 CHAOS_R3_RATE=1.0 CHAOS_R6_RATE=1.0 CHAOS_SEED=42 python app.py --question "What does the Kestrel Basin report say about sea level rise and drought?" --pdf fixtures/long_climate_report.pdf > chaos_out.json"

Then, against live SigNoz + live LLM providers (needs OPENAI_API_KEY and a locally running "ollama serve" with a model pulled, e.g. "ollama pull llama3.1"):
1. Trigger or reuse a chaos run so the audit window has real R3/R6 findings (same commands as Stage 4's guidance above work, or run a clean baseline to also exercise the "everything's clean" narrative path).
2. Copy experiments/test_stage5.py to the repo root (same as test_stage3.py/test_stage4.py) and run: "python test_stage5.py"
3. This fetches R1/R2/R3/R6 ONCE via MCP, combines them into one AuditFindings object, then calls narrative.generate_report against that SAME object once per provider (LLM_PROVIDER=openai, then =ollama) -- so any difference in the two reports is attributable to the model, not to querying SigNoz twice.

**NOTE:** OpenAI is tested but Ollama still needs to tested in future for any errors.

Stage6:
python scripts/provision_dashboards.py --dry-run
python scripts/provision_dashboards.py
Go to https://webhook.site and get the URL provided by it.
python test_stage6.py --provision-alerts --webhook-url https://webhook.site/<id_by_this_site>

# baseline (clean, before chaos)
python app.py --question "What does the Kestrel Basin report say about sea level rise and drought?" --pdf fixtures/long_climate_report.pdf > baseline_out.json
python test_stage6.py          # <-- this is the step you skipped; pushes the metric

# now chaos
CHAOS_MODE=1 CHAOS_R1_RATE=1.0 CHAOS_R2_RATE=1.0 CHAOS_R3_RATE=1.0 CHAOS_R6_RATE=1.0 CHAOS_SEED=42 \
  python app.py --question "..." --pdf fixtures/long_climate_report.pdf > chaos_out.json
python test_stage6.py          # <-- pushes a second, lower score